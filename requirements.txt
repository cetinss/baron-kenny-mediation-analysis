# Core analysis dependencies (versions used for the reported results; see
# outputs/provenance.json for the full environment of the last run).
pandas==3.0.5
numpy==2.5.2
scipy==1.18.0
statsmodels==0.14.6
matplotlib==3.11.1
seaborn==0.13.2
tabulate==0.10.0
python-docx==1.2.0

# Test suite
pytest==9.1.1

# Optional: third comparator in outputs/benchmark_report.md
pingouin==0.6.1
